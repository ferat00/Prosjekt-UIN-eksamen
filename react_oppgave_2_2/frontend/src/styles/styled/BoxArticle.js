
import styled from 'styled-components';

export const BoxArticle = styled.article`
/* each box used prop giving dynamic hexcolor from array */
/* This box for "tjenester" page article boxes */
  background-color: ${({bg})=>bg};

  
`
